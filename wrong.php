<?php
    // $insecureRandom = rand(1, 1000);
    // echo "Vencedor: $insecureRandom";

    // function customInsecureRandom($min, $max) {
    //     $seed = microtime(true) * 1000;
    //     $range = $max - $min + 1;
    //     $randomNumber = $min + ($seed % $range);
    //     return $randomNumber;
    // }
    
    // $randomNumber = customInsecureRandom(1, 100);
    // echo "Número aleatório (não seguro): $randomNumber";
?>